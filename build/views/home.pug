extends layouts/main.pug
include mixins/imageBlock

block content
    .main__feed
        p 공지) 서버 오류로 인해 소셜 로그인이 안되네요...
        .titlebar 오늘의 작업
        .grid__container
            each image in images
                +imageBlock(image)